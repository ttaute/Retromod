/*
 * Retromod — compile-time stub of
 * net.neoforged.neoforgespi.locating.ModFileDiscoveryAttributes.
 * Copyright (c) 2026 Bownlux. MIT License.
 *
 * Compile-only; stripped from the production jar. RetromodModLocator only reads
 * the static DEFAULT field — at runtime the getstatic resolves to NeoForge's
 * real field (object fields are not compile-time-inlined), so the null here is
 * never observed.
 */
package net.neoforged.neoforgespi.locating;

public final class ModFileDiscoveryAttributes {
    public static final ModFileDiscoveryAttributes DEFAULT = null;

    private ModFileDiscoveryAttributes() {
    }
}
