# Retromod NeoForge mod-file locator (#78): discovers jars in mods/Retromod/
# so Retromod + transformed *-retromod.jar outputs can ship as CurseForge pack
# overrides instead of top-level mods/ jars. Read only by NeoForge (FML early
# services); ignored by Fabric and Forge, which use different SPIs.
com.retromod.locator.RetromodModLocator
