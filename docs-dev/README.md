# Developer notes (not published)

Internal design and planning notes. This folder is intentionally OUTSIDE `docs/`
so GitHub Pages (which builds the Jekyll site from `docs/`) never publishes it.
User-facing documentation belongs in `docs/`.
